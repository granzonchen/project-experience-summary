# 旧档案时代跨项目规则（R-13~R-21）

> 证据来源：`D:\projects\projects-old` 归档项目群（2020~2024，Odoo/Python/Django/uni-app 时代）的 8 篇复盘，见 `projects/` 下 cevt、ltc、django-vue-admin-pro、dawei-dw、odoo-industry、fork-dashboard、gov-screen-gis、miniapp-cluster。
> 与 R-01~R-12 的关系：R-01~R-12 来自活跃 Java+Vue 项目（治理现状），本篇规则全部来自**已归档旧项目的事后复盘**——价值在于"这些模式当年真实付出了多少学费"。编号连续便于引用。
> 迁移性口径：每条规则都过了 METHOD.md 第四节三档过滤，达【金】或【留】档；版本特定细节留在各项目复盘内，不进入本篇。

---

## R-13 复制改造必须"改造点清单化 + 副本自洽门禁"

**现状**（≥6 项目）：
- 正面：dw 删 19 个 app 后 settings/urls 同步清理，grep 残留引用为 0；首提交前整树格式化，后续 diff 只剩真逻辑差异（dawei-dw）
- 反面：hn_mall→ext 整仓克隆、双方仅 Initial Commit，演进决策（换后端）不可追溯（miniapp-cluster）；ltc 按语言整仓复制做国际化，中英两版逐字符串手工替换、必然漂移（ltc）；zhsq community_1/zhsq_app 复制分叉、大屏"副本.htm"当版本管理（gov-screen-gis）；rfmt 脚手架批量复制把空壳伪装成"已建三模块"（odoo-industry）

**规则**：复制/裁剪/改造代码库时，四步强制：①原始拷贝即首次提交（先建 VCS 再动手）；②格式化+重命名清噪音；③改造点写清单（删了什么、改了什么、为什么）；④验收=注册点清理复验（INSTALLED_APPS/路由/菜单/前端路由）+ 全库 grep 被删名零残留 + 副本与本体 diff 门禁。

**强制要求**：语言/环境/客户差异**用构建变量表达，不做整仓副本**；复制完成后跑一遍"脚本-真实路径-真实命令"对账清单（dawei 的 docker_start.sh 挂着三个上游项目的三种路径、run-docker.sh 调不存在的命令，即此坑实物）。

---

## R-14 "能跑的状态"唯一出处是 VCS，副本/快照/dump 不是版本管理

**现状**：hn_service 7202 提交 0 tag → 目录快照四散（mms-wmp-back、pms_wewqe 漂移 60+ 文件、ltc备份、69MB zip）（ltc）；dawei 归档丢 .git、`_before.py` 整文件手工备份、44MB mysqldump 当表结构唯一来源且 .gitignore 掉全部 migrations（dawei-dw）；zhsq 快照（4216 行）与源视图（1040 行）彻底脱节无法追溯（gov-screen-gis）。正面：cevt 迁移前把三仓 commit hash 钉进 baseline/commit_log.txt，永远可回溯。

**规则**：可运行/已发布状态用 tag + 发布流水线表达；数据库表结构以迁移脚本为源（进 VCS），dump 只做种子数据；任何"基线切换"操作先把来源版本号固化进产物。

**强制要求**：归档项目先补 tag/说明再封存；发现"副本当版本管理"（-副本文件、_before 文件、back 目录）即视为债务信号。

---

## R-15 插件/依赖：来源清单是第一道门禁，上游修改 patch 文件化

**现状**：xinyi 138 addon 四源混装（企业版/OCA/商业/自研），version 字段 13/14/15 漂移不可作基线；odoo14addons 同代正面样本——OCA 原样引入 + 自研以 _ext 包裹不改上游；cevt 10 代单仓 214 addon 大杂烩被迫重付拆仓成本、vendor 的 geely_mail_fixed 直接 fork 修改（下轮升级补丁即丢）；ltc 社区 addon 直拷无清单、queue_job 引入后零引用。

**规则**：引入任何第三方插件/依赖时登记来源与基线版本；对上游的修改一律以 patch/叠加层（_ext、_inherit）表达，禁止直接改 vendor 文件；仓库第一天就按 platform / business / customer 三层分层。

**强制要求**：CI 装前校验（依赖拓扑/来源清单/版本一致性——cevt 的 dependency_checker.py + compute_module_update_order.py 是现成范式）；"引入未用"的依赖定期对账清除。

---

## R-16 环境配置模板化：一处定义 + 变量注入，环境切换不靠改代码

**现状**：cevt 一个 venv 配 8 份互拷的公司 conf 手工漂移；ltc 数据库名写死在代码里靠注释切换（httpRequest.js）、compose 写死个人 macOS 路径与私仓 IP；dvap 单 settings.py + env.py 覆盖 + `locals().get` 兜底；dawei 复制脚本路径寄生三个上游项目名。正面：dawei/dvap 的 env.example 模板思路、dvap 基础镜像预构建推私有 registry。

**规则**：环境差异（主机/库名/开关/凭据引用）只允许存在于模板 + 注入层（env 文件/环境变量/配置中心）；代码与配置文件里出现"注释掉 A 用 B"式切换即违规。

**强制要求**：新环境 = 新 env 文件，不复制配置改内容；部署文件改动必须 `compose config` 渲染校验 + 真实起容器验证（dvap 的 environment 块缩进掉层导致"修过≠生效"，git 有两次相关修复提交）。

---

## R-17 敏感态与代码同生命周期：密钥、PII、抓包快照三形态同治

**现状**（扩展 R-03"密钥零明文"到归档场景）：密钥入仓——cevt k8s secrets.yaml + 8 conf、dvap launch.sh root 口令 + README 演示账号、antai CCD 端点 auth=none + 前端硬编码管理员账密、xinyi xy.conf、dawei compose 明文 root；**PII/敏感数据随交付物滞留**——api.md 接口抓包含真实居民信息、gis.htm/bigData 快照含真实数据（gov-screen-gis）；测试资产搬运凭据——ltc-test-base 引用 cevt 三套环境地址+口令。

**规则**：三类"敏感态"同等对待：①密钥/凭据（Vault/env 注入，仓库只留引用）；②业务数据与 PII（演示用脱敏数据集，真实数据导出不进代码目录）；③交付/排错产物（抓包、页面快照、dump、日志）与代码库物理隔离。归档或交接前强制过"脱敏 + 密钥清理"清单。

**强制要求**：扫描工具（truffleHog 等）之外，人工检查三类高发位：conf/workspace/compose/README、`*.md`/抓包文件、测试脚本与 fixtures；发现问题记位置与类型，先轮换凭据再清理历史（BFG）。

---

## R-18 外部集成三件套：显式超时+重试、防腐层隔离、身份约定不硬编码

**现状**：ltc ES 客户端踩挂后才补 timeout=30+retry（git e3d5459b）；xinyi wms_sync 在 create 主事务内联同步外部系统且租户 ID 硬编码；cevt 邮件路由硬编码供应商发件人白名单——供应商改地址即静默断流，且批量导入用"收集-汇总-一次性抛错"兜住脏数据；rfmt 对接 stub 仅 timeout=3 一个参数是对的；antai 大屏前端绕开聚合接口直查明细模型（契约双轨）。

**规则**：任何外部系统调用强制三件套——①显式超时+重试（默认没有，必须显式配）；②外部写操作不进本地主事务（outbox/事件化），外部数据进门先过逐列契约校验；③外部系统的"身份约定"（地址/账号/租户标识）配置化，无匹配路由时显式告警而非静默丢弃。

**强制要求**：多消费端共享外部数据时收拢到唯一权威源/聚合接口再辐射（ltc ERP 中枢是正面样本），杜绝前端直连明细的双轨。

---

## R-19 权限三正交面分表建模，鉴权只信任服务端

**现状**：dvap 把"路由可见（Menu）/接口可调（MenuButton: api+method）/数据行可见（data_range+部门 M2M）"三面分表、服务端每请求编译 `api:method` 集合精确匹配、数据权限做成"模型声明字段即生效"的可插拔 Filter——同代最完整参考实现；反面：antai 对内设备端点全部 auth=none+cors=*，前端 mounted 硬编码管理员账密；dvap 自身数据权限过滤器因字段名漂移静默收窄（本部门及以下退化为仅本部门）。

**规则**："页面可见、按钮可点、数据可见"是三种独立权限，分开建模；前端菜单/路由控制只是 UX，不是安全边界；对内接口、设备接口同样默认拒绝。

**强制要求**：权限过滤的回归测试必须断言**可见行数/可见集合**（只测"不报错"测不出静默收窄）；行级权限优先做成声明式查询层横切，而非散在业务代码。

---

## R-20 测试资产随项目走；分层测试意图（单元/UI 留证/压测）值得整体平移

**现状**：ltc-test-base 的 RPC 测试引用 cevt 的模型与三套环境凭据，对本项目零守护还搬运了他人密钥；同项目三层测试意图齐备（Odoo 单测 tag + Selenium 截图落盘 + Locust 按权重压 RPC）；cevt 用白名单显式声明"哪些模块免 UI 测试"，覆盖有台账。

**规则**：测试基线（用例/fixture/环境地址/凭据）随项目走，复制项目时测试目录必须清空重建；保留三层意图——单元、UI（截图留证）、关键链路压测。

**强制要求**：测试脚本里出现他项目模型名/环境地址即告警；豁免清单显式化，不允许"默认没测"。

---

## R-21 双源漂移对账：注释/文档/清单与代码修一必修二

**现状**：dawei JWT 时长改值注释留旧（且跨复制项目遗传）、init 命令读 `system_t/` 与交付的 `system/` 错位靠 README 手工步骤补偿、run-docker.sh 调不存在的命令；dvap README 承诺与代码漂移（celery.py 在而依赖不在、MySQL 版本矛盾）、操作日志脱敏只覆盖顶层键；cevt 模块注册表与磁盘漂移（代码里有对账分支为证）；gov 大屏快照与源视图脱节。

**规则**：凡是"同一事实的两处表达"（注释↔数值、README↔依赖、清单↔目录、快照↔源码），修改时强制同步，或干脆消灭双源（注释不写数值语义、文档由代码生成、清单由目录生成）。

**强制要求**：接手/归档项目先按 README 跑一遍——跑不通处即债务清单；长期靠 CI 对账（清单自动生成、文档链接校验）。

---

## 附：模板化开发的得失（miniapp-cluster 结论，未成规则但需记住）

- 模板起步快，但 UI 组件库整包进生产（ThorUI demo 页骑在生产 app 三处）而**真正值得沉淀的 API 封装层（odooApi.js）却在项目间靠复制传播**——收敛方向错了一层。
- 复制迭代（整仓克隆 + 单 Initial Commit）让演进决策不可追溯，符合 R-13/R-14 双重违规。
- 低价值项目（SaaS 模板试玩、官方示例、UI kit 拼盘）的结论就是"无增量"——记录一句防止下次再当资产对待。

## 规则编号索引

| 编号 | 规则名 | 证据项目 | 优先级 |
|---|---|---|---|
| R-13 | 复制改造清单化+自洽门禁 | dawei-dw/hn_mall/ltc/zhsq/rfmt/miniapp | P0 |
| R-14 | 可运行状态唯一出处是 VCS | ltc/dawei-dw/zhsq（正面：cevt） | P0 |
| R-15 | 依赖来源清单+上游补丁文件化 | xinyi/odoo14addons/cevt/ltc | P0 |
| R-16 | 环境配置模板化一处定义 | cevt/ltc/dvap/dawei | P0 |
| R-17 | 敏感态三形态同治 | 全部 8 篇均命中 | P0 |
| R-18 | 外部集成三件套 | ltc/xinyi/cevt/rfmt/antai | P1 |
| R-19 | 权限三正交面+服务端鉴权 | dvap（正面+反面）、antai | P1 |
| R-20 | 测试资产随项目走+三层意图 | ltc（反+正）、cevt | P1 |
| R-21 | 双源漂移对账 | dawei/dvap/cevt/gov | P1 |

## 新项目启动检查清单（补充 GLOBAL_RULES 第七节）

```
[ ] 复制/裁剪改造：改造点清单 + grep 残留复验 + 副本 diff 门禁（R-13）
[ ] 发版用 tag，禁用目录快照/_before 文件表达"能跑状态"（R-14）
[ ] 第三方依赖登记来源清单，上游修改走 patch/叠加层（R-15）
[ ] 环境差异全部走 env 注入，代码里无"注释切换"（R-16）
[ ] 归档/交付前过脱敏清单：密钥、PII、抓包快照三类（R-17）
[ ] 外部调用显式超时+重试，外部写不进主事务（R-18）
[ ] 权限三面分表；权限回归断言可见行数（R-19）
[ ] 测试基线随项目走，无跨项目凭据引用（R-20）
[ ] 注释/文档/清单与代码无双源漂移；接手先跑 README（R-21）
```
