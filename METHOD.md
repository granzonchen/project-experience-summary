# 项目经验提炼方法论（METHOD）

> 本文回答"如何从项目（尤其是已归档/技术栈落伍的旧项目）中总结归纳经验教训、提炼规则与方法论"。
> 它是本仓库所有复盘与规则的分析标尺：单项目复盘按第五节模板写，跨项目规则按第六节模板写。
> 适用场景：项目归档时、阶段性交付后、重大踩坑修复后、同类项目群（同源/复制改造）整体清算时。

---

## 一、核心原则（三条，优先级从高到低）

### 1. 证据链原则：每条经验必须能指回证据

不接受"印象流总结"。每条亮点/坑至少要有一种证据：

| 证据类型 | 例子 | 强度 |
|---|---|---|
| git 历史修复记录 | `git log --grep="fix"` 得到的真实翻车与修复 | 最强（真实发生） |
| 代码中的防御性写法 | 防重复提交注解、getInstanceByDom 兜底 | 强（团队交过学费） |
| 配置/脚本/文档残留 | 部署脚本、handbook、交接笔记 | 中（意图可见） |
| 结构性事实 | 多副本目录、同源项目 diff、依赖版本分裂 | 中（可测量） |
| 推断（需标注） | "疑似因 X 导致 Y" | 弱（必须写明是推断） |

### 2. 迁移性优先：写到"模式级"，不写"报错级"

技术栈会过时（Odoo 10 → 18、Vue2 → Vue3、Django 3 → 5），但模式不会。
复盘的产出单位是**可迁移的模式**，不是某个版本某行的修法。判断问句：

> **"换一门语言 / 换一个框架，这条经验还成立吗？"**
> - 原样成立 → 金级（跨时代工程判断）
> - 改写表述后成立 → 留（可迁移设计思想）
> - 不成立 → 弃（版本特定细节，一句话带过即可）

### 3. 分级沉淀：经验按成熟度分流，不一步到位

不是所有经验都配得上"规则"。先入池、再成文、后成规：

```
原始观察（一句证据+一句猜想）
  → 项目复盘（有证据链的结构化总结，本文档仓库 projects/）
    → 跨项目规则（≥2 个项目重复出现 + 迁移性达标，GLOBAL_RULES.md）
      → 强制规范（接入 CI/钩子/skill 可自动检测，如 lessons-from-cwjt skill）
```

跳级警惕：只有一个项目支撑的"规则"是猜测；没有检测手段的"强制规范"是口号。

---

## 二、三层漏斗总流程

```
L1 项目复盘        What happened   —— 每个有价值项目一份，按第五节模板
      ↓
L2 模式提取        What repeats    —— 跨项目聚类：同源对照、时间线对照、重复坑归并
      ↓
L3 迁移性过滤      What survives   —— 三档分流（第四节），只有"留"和"金"进入规则层
```

**L2 的三个聚类视角**（缺一不可，尤其前两个最容易被忽略）：

1. **同源对照**：同一模板/同一代码库复制出的项目两两对比（如 dawei→dw、hn_mall→hn_mall_ext、9 副本 JeecgBoot）。复制的收益（起步快）与代价（改一处漏 N 处）都是经验。
2. **时间线对照**：同一业务在不同时期的项目（旧栈项目 vs 新栈项目，如 Vue2 JeecgBoot 旧版 vs Vue3 monorepo 重写版），看哪些问题换代后依然复发——复发的就是"金"。
3. **重复坑归并**：不同项目踩的同一类坑（如硬编码密钥、租户 ID 硬编码、定时器泄漏）。出现 ≥2 次才够格进 GLOBAL_RULES。

---

## 三、项目清点与价值分级（漏斗入口）

动手复盘前先清点，避免在低价值项目上花时间：

1. **盘点**：识别每个目录是真实项目 / 复制品 / 模板 / 学习代码 / 第三方源码 / 纯文档。
2. **画像**：类型、技术栈、规模（模块数/文件数）、最后修改时间、有无文档与 git 历史。
3. **分级**：
   - **第一梯队（深挖）**：业务复杂 + 有完整前后端 + 踩坑密度高（有 git 修复记录/大量防御性代码）。
   - **第二梯队（合并简评）**：单一业务线、模板化程度高，但能贡献 1~2 个模式级结论；同源项目合并成一篇对照着写。
   - **第三梯队（仅列清单）**：第三方源码、官方示例、纯学习代码——一句话记录"为什么没价值"也是防复蹈的经验。
4. **红线先查**：动笔前先扫敏感信息（明文密码、密钥、PII、隐私材料）。**复盘文档本身绝不能成为新的泄露源**——只写"此处存在敏感信息，需脱敏"，不复制内容。

---

## 四、迁移性三档判断标准（漏斗出口）

| 档 | 定义 | 典型例子 | 去处 |
|---|---|---|---|
| **金**（跨时代工程判断） | 与语言/框架无关的工作方式与判断准则 | 验证铁律（代码≠运行实例）、复制改造先收敛依赖、密钥零明文、核心流程双人备份 | GLOBAL_RULES 强制规范 / AGENTS.md / skill |
| **留**（可迁移设计思想） | 表述泛化后跨栈成立的设计模式 | 聚合接口+并行查询、状态机显式化、阈值配置化下发、Mock 先行、快照刷新 | GLOBAL_RULES 建议项 / 项目复盘亮点区 |
| **弃**（版本特定细节） | 绑定具体版本 API/文件路径的修法 | "Odoo 14 的 X 字段要 Y"、"Vue2 的某修饰符" | 复盘内一句话留痕，不进规则层 |

**泛化改写示范**：

- ❌ 弃级表述："Odoo addon 的 manifest 里 depends 写错版本号导致装不上"
- ✅ 留级表述："二开平台的模块依赖必须显式声明且锁定基线版本，装前用清单校验"
- ✅ 金级表述："任何插件/扩展机制，依赖清单是第一道门禁——先机器校验依赖，再谈安装"

---

## 五、单项目复盘模板（L1 产出物）

```markdown
# {项目名} - 项目经验总结

> 分析对象：{绝对路径}（规模、最后修改时间）
> 分析方式：只读代码勘察（+ git 历史 / 文档勘察，注明日期）

## 一、项目背景与业务问题
当年要解决什么业务问题（1~3 句）、业务闭环是什么、给谁用。
—— 目的：让"思路"有语境；脱离业务语境的架构总结无法迁移。

## 二、架构与技术决策
怎么拆模块、怎么集成外部系统、怎么部署；每个关键决策附"为什么"。

## 三、做对了什么（可迁移思路）
标注迁移档位【金/留】。每条：模式名 + 证据（文件/提交）+ 一句话泛化表述。

## 四、栽了什么坑（模式级教训）
坑的**模式**而非报错原文：触发条件 → 为什么会这样 → 防范模式。
有 git 修复记录的优先引用；纯推断的标注"推断"。

## 五、若用现代栈重做
当年这个项目的思路，今天用主流栈怎么落地（1~5 条）。
—— 这是"栈落伍、思路不过时"的兑现处。

## 六、证据索引
关键文件路径、handbook/脚本位置、敏感信息位置（只记位置，不记内容）。
```

**写作纪律**：
- 重量级思路、轻 API 细节：第三、四、五节合计篇幅应 ≥ 全文一半。
- "坑"写到模式级：不贴报错堆栈（那是 skill 层的事），写"什么情况下人会掉进去"。
- 同源项目合并复盘：单独一节"对照表"列差异，教训写在差异里。

---

## 六、跨项目规则模板（L2+L3 产出物）

沿用 GLOBAL_RULES 的**治理型三段式**：

```markdown
### R-XX {规则名}
**现状**（≥2 个项目的证据）
**规则**（一句可执行的要求，写成"禁止 X / 必须 Y"）
**强制要求**（怎么落地：检查时机、检测命令/工具、违反后果）
```

若坑属于"会炸的故障"而非"治理缺失"，改用 `lessons-from-cwjt` skill 的**故障型七段**：
症状（真实报错）→ 根因 → 最小复现 → 正确写法 → 检测手段 → 跨项目适用性 → 一句话总结。

**规则准入门槛**（三条全过才写入 GLOBAL_RULES）：
1. ≥2 个独立项目有证据；
2. 迁移性达"留"或"金"档；
3. 有可执行的检测/落地方式（哪怕是 grep 一条命令）。

---

## 七、沉淀位置分流表

| 经验形态 | 去处 | 理由 |
|---|---|---|
| 单项目复盘（含同源对照） | 本仓库 `projects/{项目}.md` | 证据链跟项目走 |
| 跨项目规则（R-XX） | 本仓库 `GLOBAL_RULES.md` | 供新项目启动清单引用 |
| 会"炸"的故障级坑 | 用户级 skill（lessons-from-* 系列） | 需要 AI 会话自动触发才防复发 |
| 个人工作方式纠正 | AGENTS.md / agent-hub `canonical/rules/behavior.md` | 跨会话必读才能生效 |
| 领域方法论（如何做某类事） | agent-hub `knowledge/domain/` 或本仓库 METHOD 类文档 | 可复用流程 |
| 未成形的原始观察 | 经验原始池（agent-hub experiences/ 或项目内备忘） | 攒够证据再升级 |

---

## 八、产出质量自检清单（提炼完成后过一遍）

```
[ ] 每条亮点/坑都有证据指针（文件路径 / git 提交 / 文档位置）？
[ ] 规则级结论至少有 2 个项目支撑？
[ ] 用"换栈还成立吗"问句过完三档分流，没有版本细节混进规则层？
[ ] 复盘第三/四/五节（思路类）篇幅 ≥ 一半？
[ ] 敏感信息零复制（密码/密钥/PII 只记位置不记内容）？
[ ] 同源项目做了对照而不是各写各的？
[ ] "若用现代栈重做"一节能直接指导今天的新项目？
[ ] 沉淀位置按第七节分流表放对地方（该进 skill 的没堆在文档里）？
```
